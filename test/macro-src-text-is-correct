#!/bin/bash
#
# Ensure that ASTs generated by a macro have correct source test.
#
. $(dirname $0)/common

test "$(run_macros -json | json_filter counter 3 | jshon -e src_text -u)" = \
     "MULTI_STATEMENT_MACRO"
